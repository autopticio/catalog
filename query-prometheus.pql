where("$promOnAws")
  .what("node_cpu_seconds_total")
  .when("1h")
.window("15m")

.alias("$where[0].what[0].when[0].window[0]").as("p_cpu")
.list("$p_cpu")
.out("./data/query-prometheus.json", "./logs/query-prometheus.txt")

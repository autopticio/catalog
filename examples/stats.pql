where("$cw_aws")
.what("MetricName='CPUUtilization';InstanceId='i-00f8880d7a4d502db'; Namespace='AWS/EC2'")
.when("24h")
.alias("$where[0].what[0].when[0]").as("data")

.average("$data")
.min("$data")
.max("$data")
.count("$data")
.percentile("$data; 0.25; 0.5; 0.75; 0.99")

.out("stats.json")

where("$cw1")
.what(
    "CPUUtilization; Average; InstanceId='i-00f8880d7a4d502db'; namespace='AWS/EC2'"
)
.when("1h")
.window("15m")
.alias("$where[0].what[0].when[0].window[0]").as("p_cpu")

.print("$p_cpu")
.out("./data/query--cloudwatch.json", "./logs/query--cloudwatch.txt")
